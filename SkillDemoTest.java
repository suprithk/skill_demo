import static org.junit.Assert.*;
import org.junit.*;

public class SkillDemoTest {
    @Test
    public void testEven() {
        assertTrue(SkillDemo.isEven(4)); // should be true now
    }
}

// I haven't commited and pushed these changes yet, so we have to do that first